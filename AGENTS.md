# ZzCode AGENTS

## 项目定位

ZzCode 是用于学习的 Python Code Agent CLI。目标是分阶段复刻 Claude Code 的核心机制，先保证小步可运行，再逐步完善。

## 架构边界

* 使用 Python `src/` 布局。
* CLI 只负责配置加载、依赖组装、交互启动。
* Agent Core 负责 LLM、工具调用、上下文和执行循环。
* 终端 UI 与 Agent Core 解耦；React + Ink 前端通过 JSON Lines 事件协议通信。
* 工具通过注册表管理；后续 MCP 作为工具来源接入。
* LLM Provider 使用统一小接口隔离模型厂商。
* 优先使用标准库，必要时再引入第三方依赖。

## 执行规则

* 先思考后行动。
* 每一步执行前先写出 plan，再按 plan 执行。
* 每个步骤执行前先回忆 Claude Code 的原始实现，优先按 Claude 的实现思路落地。
* 当前阶段只做必要改动，避免大范围重构。
* 修改项目约定时，同步更新本文件。
* 边做边学时，先解释设计原因，再进入实现。

## 阶段规则

* 每个阶段先明确目标、验收标准和暂不实现内容。
* 每个阶段在 `docs/` 下维护对应 Markdown 文档。
* 当前优先级：ReAct + Tool Call CLI demo → 安全工具/配置/测试 → Plan 模式 → Memory/上下文管理 → MCP。

## 注释规则

* 轻量注释，不逐行解释。
* 只给关键类、关键函数、核心流程和不直观逻辑加注释。
* 关键函数顶部用简短中文 docstring，说明用途、主要参数和返回值。
* 复杂逻辑前用 1 到 3 行中文注释，解释设计原因。
* 不注释 import、简单赋值、简单 if、简单 return。
* 注释保持简洁，不改变代码逻辑。
